#include <bits/stdc++.h>

using namespace std;

string stringGameWinner(string s, string p){
    // Complete this function
}

int main() {
    int q;
    cin >> q;
    for(int a0 = 0; a0 < q; a0++){
        string s;
        cin >> s;
        string p;
        cin >> p;
        string result = stringGameWinner(s, p);
        cout << result << endl;
    }
    return 0;
}

